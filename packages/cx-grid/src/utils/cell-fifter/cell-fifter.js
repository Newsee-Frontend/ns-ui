export default {
  /**
   * grid cell fifter function
   * @param val
   * @param key
   * @returns {*}
   */
  cellFifter: (val, key) => {
    return val;
  },
  /**
   * test
   * @param val
   * @param key
   * @returns {*}
   */
  test: (val, key) => {
    return val;
  }
}
